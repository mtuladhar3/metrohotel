import FooterSection from '@/components/layout/footer/FooterSection';

/** Shared site footer, rendered by the root layout on every route. */
export default function Footer() {
  return <FooterSection />;
}
